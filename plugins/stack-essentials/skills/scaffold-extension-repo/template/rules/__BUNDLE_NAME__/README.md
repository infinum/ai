# __BUNDLE_NAME__

TODO — one sentence shown by the installer as this bundle's prompt hint (this line, the first non-heading line, is what users see when choosing bundles).

Files in this folder are CLAUDE.md-style instructions. They are installed to `~/.claude/infinum/__BUNDLE_NAME__/` for people who opt in with `--bundles __BUNDLE_NAME__`, and load in every Claude Code session. Keep each file on one topic, short, and concrete. Rule file names must be unique across all sources a person installs, so keep the `__NAME_PREFIX__-` prefix.
