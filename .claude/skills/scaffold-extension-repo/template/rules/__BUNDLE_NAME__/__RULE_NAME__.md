# __RULE_NAME__

TODO — the conventions the agent should always follow for the __OWNER_LABEL__. Write them as short imperative statements, with the *why* where it is not obvious. Example shape:

- Prefer X over Y because Z.
- Never do A in B; do C instead.
